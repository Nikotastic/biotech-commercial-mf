# Configuración Local - Biotech Commercial MF

Este archivo documenta la configuración para ejecutar el microfrontend de Comercialización en un entorno local.

## 🚀 Ejecución

Para levantar el proyecto localmente:

1. Instalar dependencias:
   ```bash
   npm install
   ```
2. Ejecutar servidor de desarrollo:
   ```bash
   npm run dev
   ```

El servidor iniciará en el puerto configurado (usualmente 5003 o similar, verificar `vite.config.js`).

## 🛠 Notas

- Asegúrate de que el Shell esté corriendo si necesitas probar la integración completa.
- Las variables de entorno deben configurarse en un archivo `.env` en la raíz si son requeridas.
